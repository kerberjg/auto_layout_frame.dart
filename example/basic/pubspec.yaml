name: package_example
description: A template Dart package example project, to be used in the `example/` directory.
version: 0.0.1

publish_to: none

license: MPL-2.0

environment:
  sdk: '>=3.0.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  auto_layout_frame:
    path: ../../

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
